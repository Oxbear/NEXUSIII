{
  "name": "Nexus Node Dev",
  "build": { "dockerfile": "Dockerfile" },
  "settings": { "terminal.integrated.shell.linux": "/bin/bash" },
  "extensions": ["golang.Go"],
  "postCreateCommand": "go build -o nexus-node . && ./nexus-node"
}
